#include <stdio.h>

int main()
{
    char str[100];
    int i;
    printf("Enter the first number in the form ai + b: ");
    scanf("%s %d", str, &i);
    
    printf("Enter the second number in the form ai + b: ");
    scanf("%s %d", str, &i);
    
    


    return 0;
}

